{{#if success_message}}
    <div class="alert-success">
        {{success_message}}
    </div>
{{/if}}
{{#if error_message}}
    <div class="alert-danger">
        {{error_message}}
    </div>
{{/if}}
